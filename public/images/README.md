# Images needed in this folder

Add these two files here (exact filenames matter):

- **owner-dashboard.jpg** — landscape, ~1200x800px. Suggested: an owner/manager
  reviewing a tablet or laptop showing a menu or chart.
- **kitchen-dashboard.jpg** — landscape, ~1200x800px. Suggested: a kitchen scene —
  order tickets on a rail, or a chef glancing at a screen.

Used by `app/portal/page.jsx` (the workspace-picker page shown right after login).
If either file is missing, that card gracefully falls back to a colored icon instead
of a broken image — nothing breaks, it just won't look as polished until you add them.
